# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environment
.venv/
venv/

# OS / editor
.DS_Store
*.swp

# Secrets — never commit
.env
*.key

# Claude Code machine-local settings (personal permissions, not project config)
.claude/settings.local.json

# Generated artifacts (charts regenerate deterministically; keep tree clean)
outputs/*.png
outputs/*.html
!outputs/.gitkeep

# Generated website (rebuild with `python website.py --build`)
site/

# UX-audit scratch (evidence notes + driver scripts)
.audit/

# Generated evaluation artifacts (regenerate with `python -m evaluation.extraction_eval`)
evaluation/figures/
evaluation/REPORT.md
evaluation/FORECAST_REPORT.md

# Ingested analyst-call dataset (regenerate with `python -m analyst_scorecard.ingest.pipeline`)
data/analyst_calls.jsonl
